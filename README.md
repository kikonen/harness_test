# LLM Harness

Experimenting simple harness logic. Autogenerated, so don't expect much.

## Setup

Copy env and add TOKEN into .env

```bash
cp -a _env .env
```

## Usage

```bash
./run.sh [prompt]
```
